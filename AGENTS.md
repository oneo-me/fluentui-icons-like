## 规则

- 不要保留向后兼容性。移除已过时的路径，而不是添加兼容层、回退方案或迁移。
- 选择最简单、且能完全满足当前需求的实现。避免投机性的抽象、配置和间接层。
- 按层扩展系统。从端到端可工作的最小版本开始，在已经能正常运行的产品之上逐步添加新能力。永远不要用尚未完成的复杂性去替换一个可工作的产品。
- 保持组件模块化，并清晰地分离关注点。
- 当既有、维护良好的库能降低整体复杂度或提升可靠性时，优先使用它们。除非有明确理由，否则不要重复实现通用功能。
- 在编写自己的实现或引入新包之前，优先利用项目中已有的依赖。不要在未查阅其文档和类型的情况下就假设某个库缺乏某项能力。
- 面向长期做架构决策。不要接受那种仅对当前有效、并计划在之后替换的权宜之计。

## 项目文档

本项目的文档根为 `locus://projects/fluentui-icons-like`，跨项目协作规范入口为 `locus://standard/development`，项目子规范入口为 `locus://projects/fluentui-icons-like/standard`。

每个对话都必须读取一次 `locus://standard/development`，并读取 `locus://projects/fluentui-icons-like/standard`；同一对话内已读取过则复用，不重复读取。项目子规范未写内容时，继续遵循跨项目规范。

此后按照上述规范，围绕当前任务读取和同步 `locus://projects/fluentui-icons-like` 下职责对应的笔记；任务完成前先核对是否需要同步。无法访问 Locus 时明确说明，不假装已经读取或同步。

访问或更新 Locus 笔记时，先从当前项目配置解析文档库中的原始笔记路径，再直接读取或用补丁更新原文件。不要先在项目目录、`/tmp` 或其他位置创建笔记副本、临时补丁或中间文件再搬运到文档库。

## Git 提交

- 用户明确要求提交时，提交范围同时包含代码和与本次变更对应的项目笔记；分别在代码仓库与笔记仓库按 `locus://standard/commit` 核对、提交并推送，不再询问。
- 除用户明确要求或已推送历史确需修正外，不使用强制推送（`--force` / `--force-with-lease`），不改写已推送历史。
